<h1>検索結果</h1>

<p>合計：<%= @total %>件</p>

<% @rooms.each do |room| %>
  <div>
    <%= link_to room.name, room_path(room) %>
    <p>住所：<%= room.address %></p>
    <p><%= room.description %></p>
    <p>料金：<%= room.price %>円/日</p>
  </div>
<% end %>